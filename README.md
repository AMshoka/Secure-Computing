# Secure-Computing
All data in this database is encrypted using the RSA encryption algorithm. Then on the client side, the sent
queries are checked and sent to the meta data table. then the encrypted data is returned, filtered, decrypted and
delivered to the use. Method and materials are available.
